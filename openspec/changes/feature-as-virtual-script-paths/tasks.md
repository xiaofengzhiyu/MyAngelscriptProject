## 1. Virtual Path Value Model

- [ ] 1.1 <!-- TDD --> Add focused runtime integration tests for canonical `as://` parsing and module-name derivation. Scope: create `Plugins/Angelscript/Source/AngelscriptTest/FileSystem/AngelscriptVirtualScriptPathTests.cpp`; cover valid project/plugin/memory/mod paths, invalid path rejection, project legacy module names, and prefixed non-project module names. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.FileSystem.VirtualScriptPaths" -Label virtual-script-paths-value-red -TimeoutMs 600000`.
- [ ] 1.2 <!-- TDD --> Implement the virtual path value type and source descriptor. Scope: add focused runtime headers/source under `Plugins/Angelscript/Source/AngelscriptRuntime/Core/` such as `AngelscriptVirtualScriptPath.h/.cpp` and `AngelscriptScriptSource.h/.cpp`; expose normalization, validation, mount kind, mount name, relative path, display path, and module-name derivation. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.FileSystem.VirtualScriptPaths" -Label virtual-script-paths-value-green -TimeoutMs 600000`.
- [ ] 1.3 <!-- Non-TDD --> Build the editor target to catch public/private include and module dependency mistakes introduced by the new runtime files. Scope: `Plugins/Angelscript/Source/AngelscriptRuntime/AngelscriptRuntime.Build.cs` if new include paths or source placement require adjustment. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label virtual-script-paths-value-build -TimeoutMs 180000`.

## 2. Source Provider Registry

- [ ] 2.1 <!-- TDD --> Add provider-registry tests for built-in disk sources, external memory provider contribution, invalid provider source rejection, duplicate virtual path conflicts, and duplicate module-name conflicts. Scope: extend `AngelscriptVirtualScriptPathTests.cpp` or split to `Plugins/Angelscript/Source/AngelscriptTest/FileSystem/AngelscriptVirtualScriptProviderTests.cpp`. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.FileSystem.VirtualScriptPaths" -Label virtual-script-paths-provider-red -TimeoutMs 600000`.
- [ ] 2.2 <!-- TDD --> Implement an engine-owned virtual script source registry and provider interface. Scope: add runtime types under `AngelscriptRuntime/Core/`; add `FAngelscriptEngine` ownership/accessors; built-in project/plugin providers should adapt current `DiscoverScriptRoots()` behavior without changing default project-only compatibility. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.FileSystem.VirtualScriptPaths" -Label virtual-script-paths-provider-green -TimeoutMs 600000`.
- [ ] 2.3 <!-- TDD --> Connect source providers to the existing engine extension registry replay/attach/unregister flow so extensions can contribute providers without editing `DiscoverScriptRoots()` and providers do not leak to later engines after unregister. Scope: runtime extension registry integration and tests using a small test extension/provider, covering the modified `as-engine-extension-registry` requirements. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.FileSystem.VirtualScriptPaths.ExtensionRegistry" -Label virtual-script-paths-extension-provider -TimeoutMs 600000`.

## 3. Discovery And Preprocessor Integration

- [ ] 3.1 <!-- TDD --> Add discovery pipeline tests proving the engine enumerates source descriptors for project, plugin, and memory sources and preserves current project-only discovery compatibility. Scope: `AngelscriptVirtualScriptPathTests.cpp` plus dependency-injection helpers based on `AngelscriptScriptRootDiscoveryTests.cpp`. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.FileSystem.VirtualScriptPaths" -Label virtual-script-paths-discovery-red -TimeoutMs 600000`.
- [ ] 3.2 <!-- TDD --> Add descriptor-aware discovery APIs while retaining `FFilenamePair` compatibility adapters. Scope: `FAngelscriptEngine::FindAllScriptFilenames`, a new descriptor enumeration API, and any helper adapters needed by current call sites. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.FileSystem.VirtualScriptPaths" -Label virtual-script-paths-discovery-green -TimeoutMs 600000`.
- [ ] 3.3 <!-- TDD --> Add preprocessor tests proving descriptor-backed sources carry virtual paths through file summaries and memory-backed sources can preprocess without physical paths. Scope: create or extend `Plugins/Angelscript/Source/AngelscriptTest/Preprocessor/AngelscriptVirtualScriptPathPreprocessorTests.cpp`. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.Preprocessor.VirtualScriptPaths" -Label virtual-script-paths-preprocessor-red -TimeoutMs 600000`.
- [ ] 3.4 <!-- TDD --> Add descriptor-aware `FAngelscriptPreprocessor::AddSource` or equivalent, and extend preprocessor file/module summaries with virtual path fields. Scope: `AngelscriptPreprocessor.h/.cpp`, `FAngelscriptPreprocessorSummary`, `FAngelscriptModuleDesc::FCodeSection`, and compatibility construction from current engine context. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.Preprocessor.VirtualScriptPaths" -Label virtual-script-paths-preprocessor-green -TimeoutMs 600000`.

## 4. Compile, Diagnostics, And Events

- [ ] 4.1 <!-- TDD --> Add compile tests proving project virtual sources compile under legacy module names, non-project virtual sources compile under prefixed names, and duplicate module conflicts fail before preprocessing. Scope: FileSystem/Preprocessor themed tests using `FAngelscriptEngineScope` and existing compile/execute helpers. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.FileSystem.VirtualScriptPaths" -Label virtual-script-paths-compile-red -TimeoutMs 600000`.
- [ ] 4.2 <!-- TDD --> Route initial compile and manual compile paths through descriptor-aware preprocessing while preserving old project behavior. Scope: `FAngelscriptEngine::InitialCompile`, `CompileModules`, module lookup by filename/module, and any helper that still assumes only `RelativeFilename`. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.FileSystem.VirtualScriptPaths" -Label virtual-script-paths-compile-green -TimeoutMs 600000`.
- [ ] 4.3 <!-- TDD --> Add diagnostics/event tests proving user-visible diagnostics, preprocessing summaries, and compilation events include virtual paths while retaining physical paths for disk sources. Scope: `Plugins/Angelscript/Source/AngelscriptTest/Preprocessor/` and existing compiler-event tests if needed. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.Preprocessor.VirtualScriptPaths" -Label virtual-script-paths-diagnostics-events -TimeoutMs 600000`.
- [ ] 4.4 <!-- TDD --> Extend diagnostics and compilation event payloads to carry virtual path identity. Scope: diagnostic structs/maps in `AngelscriptEngine`, `FAngelscriptPreprocessorSummary`, `FAngelscriptCompilationEvents`, and logging/source section mapping where virtual-only sources have no physical filename. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.Preprocessor.VirtualScriptPaths" -Label virtual-script-paths-observability -TimeoutMs 600000`.

## 5. Hot Reload And Editor Mapping

- [ ] 5.1 <!-- TDD --> Add hot-reload mapping tests proving physical project/plugin file changes map to virtual source descriptors and memory-backed sources are not implicitly reloaded by disk watcher events. Scope: add a focused virtual-path watcher test file under `Plugins/Angelscript/Source/AngelscriptEditor/Tests/` or extend `AngelscriptDirectoryWatcherRootResolutionTests.cpp`; use prefix `Angelscript.Editor.VirtualScriptPaths.*`. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.Editor.VirtualScriptPaths" -Label virtual-script-paths-editor-red -TimeoutMs 600000`.
- [ ] 5.2 <!-- TDD --> Implement physical-to-virtual source mapping for reload queues. Scope: `AngelscriptEditor/HotReload/AngelscriptDirectoryWatcherInternal.*`, `FAngelscriptEngine` reload queue structures, and source lookup helpers; preserve physical directory watcher registration for disk-backed roots. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.Editor.VirtualScriptPaths" -Label virtual-script-paths-editor-green -TimeoutMs 600000`.
- [ ] 5.3 <!-- Non-TDD --> Build the editor target after runtime/editor integration to catch cross-module include and editor-only dependency issues. Scope: runtime/editor module build files only if needed. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label virtual-script-paths-editor-build -TimeoutMs 180000`.

## 6. Documentation And Final Validation

- [ ] 6.1 <!-- Non-TDD --> Update documentation that explains script roots, module naming, hot reload roots, and extension/mod entry points. Scope: `AGENTS.md`, `AGENTS_ZH.md`, `Documents/Guides/Test.md` only if test entry guidance changes, `Documents/Knowledges/ZH/RT_HotReload.md`, `Documents/Knowledges/ZH/Type_Preprocessor.md`, and any targeted guide that currently says module identity is only root-relative filename. Verify by running `rg -n "AllRootPaths|ScriptRoot|RelativeFilename|virtual path|as://|module name" AGENTS.md AGENTS_ZH.md Documents/Guides Documents/Knowledges/ZH`.
- [ ] 6.2 <!-- Non-TDD --> Run focused regression suites for the new feature after implementation. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.FileSystem.VirtualScriptPaths" -Label virtual-script-paths-filesystem-final -TimeoutMs 600000` and `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.TestModule.Preprocessor.VirtualScriptPaths" -Label virtual-script-paths-preprocessor-final -TimeoutMs 600000`.
- [ ] 6.3 <!-- Non-TDD --> Run affected broader suites after focused tests pass. Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTestSuite.ps1 -Suite Smoke -LabelPrefix virtual-script-paths-smoke -TimeoutMs 600000` and `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTests.ps1 -TestPrefix "Angelscript.Editor.VirtualScriptPaths" -Label virtual-script-paths-editor-final -TimeoutMs 600000`.
- [ ] 6.4 <!-- Non-TDD --> Run OpenSpec validation before marking the change ready for apply/archive. Verify with `openspec validate "feature-as-virtual-script-paths" --strict --json`.
