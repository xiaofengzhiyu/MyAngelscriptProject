## Why

`AngelscriptUHTTool` 现在只把直绑函数表 emit 到 `AngelscriptRuntime` 一个模块。`AngelscriptHeaderSignatureResolver.HasLinkableExport` 因此把所有"非 `AngelscriptRuntime` 模块、且 C++ 实现没有 `<MODULE>_API` / 非 inline / 非 constexpr"的 `BlueprintCallable` 函数判为 `unexported-symbol` → 退化成 `ERASE_NO_FUNCTION()` stub,运行时被迫走 `BlueprintCallableReflectiveFallback`(虽有 `FReflectiveParamCache`,作者注释自陈仍比直绑慢 3-6×)。`AS_FunctionTable_SkippedReasonSummary.csv` 中 `unexported-symbol` 行就是这部分今天本可以直绑、却被链接边界挡住的函数。Verse 在引擎内已用 UHT 把 `FVerseCallableThunk` emit 进函数所在模块自己的 OutputDirectory、再通过 CoreUObject 中的 `RegisterVerseCallableThunks` 中央 API 落到 UClass 元数据上(每个引擎模块本就 link CoreUObject,无需新增依赖),AS 完全可以同形复用 — **但因为我们不能修改引擎源码,会合点改用 UE 现成的 `IModularFeatures`(同样在 Core)**。

## What Changes

- **AngelscriptUHTTool 输出形态**:`AngelscriptFunctionTableExporter` 从 `ModuleName="AngelscriptRuntime"` 单点输出,扩展为按 supported module 分发,每个目标模块在自己的 OutputDirectory 下额外 emit 一份 `AS_FunctionTable_<Module>_CrossModule_*.cpp`(沿用现有 `AS_FunctionTable_*.cpp` CppFilters 前缀,避免被引擎 `CodeGen` exporter `CullOutput` 误删)。
- **跨模块中央会合点 — `IModularFeatures`(Core)**:每个引擎模块 emit 的 cpp 在 DLL 加载时通过静态构造把一个 POD-payload feature 注册到 `IModularFeatures::Get()`,字符串 ID 为 `"AngelscriptCrossModuleBindings"`。AS Runtime 在 `EOrder::Late + 60` 阶段调用 `GetModularFeatureImplementations(...)` 一次性拉取,**完全不 link 任何引擎模块**(`IModularFeatures` 在 Core,所有引擎模块与 AngelscriptRuntime 本就依赖 Core)。后到的模块通过 `OnModularFeatureRegistered` 委托补注册。
- **跨模块 ABI(POD payload + reinterpret_cast 反向 cast)**:新增公共头 `Plugins/Angelscript/Source/AngelscriptRuntime/Public/UHT/AngelscriptCrossModuleBindings.h`,定义 `FAngelscriptCrossModuleEntry { const TCHAR* ClassName; const TCHAR* FunctionName; void(*Thunk)(UObject* Self, void** Args, void* Ret); uint16 ArgCount; uint16 RetSize; uint32 Flags; }` 与 layout-matching 的 `FAngelscriptCrossModuleFeatureReader`、`LayoutVersionExpected` 常量、双端 `static_assert(sizeof)` 校验。**该头只被 AS Runtime 自己 include**;引擎模块 emit cpp 的同型 layout 由 generator 通过模板字符串保证两端一致,不反向依赖 AS Runtime 的 Public 头。
- **Thunk 签名为 raw 形态,不暴露 AS SDK**:emit cpp 的 thunk 形态是 `void(*)(UObject* Self, void** Args, void* Ret)`,**只 include `Features/IModularFeatures.h` 与目标类头**,不 include `angelscript.h` 也不 include 任何 AS Runtime 头。`asIScriptGeneric*` ↔ raw 缓冲区的桥接由 AS Runtime 端一个 *单一通用 generic hook* `void(*)(asIScriptGeneric*)` 完成 — 它从 generic 拿 Self/Args/Ret 放进 raw buffer,再调 entry 的 thunk;所有 cross-module entry 复用同一个 hook,通过 AS user-data 携带 entry 指针。
- **AngelscriptRuntime 端注入**:新增 `Bind_CrossModuleDirect.cpp`,`EOrder::Late + 60` 阶段执行:`IModularFeatures::Get().GetModularFeatureImplementations(FName("AngelscriptCrossModuleBindings"))` → 逐个 `reinterpret_cast` 到 reader struct → 校验 `LayoutVersion` magic → 通用 generic hook 注册到 AS Engine(`asCALL_GENERIC`)→ 写回 `FAngelscriptBinds::ClassFuncMaps` 空槽。同时 `OnModularFeatureRegistered` 处理后到模块。
- **HasLinkableExport 收敛**:不再因为"跨模块 + 无 API 宏"判 `unexported-symbol`,改为只在"目标模块未启用编译"或"`Private/`/`CustomThunk`/`Interface`"等 *真正* 不可达的情况下打 stub。
- **RPC / Net 函数继续走反射 fallback**:`FunctionFlags & (FUNC_Net | FUNC_NetServer | FUNC_NetClient | FUNC_NetMulticast)` 命中的 UFunction **绝不** emit cross-module direct bind。原因:`UFunction::Invoke` / `ProcessEvent` 内部按 `FUNC_Net` 路由 RPC,raw thunk 直接 `static_cast<T*>(Self)->Func()` 会绕过路由,把"应 marshal 到对端"的语义变成"本地直接执行函数体",**破坏网络复制**。RPC 函数继续走 `BlueprintCallableReflectiveFallback`,行为不变。
- **复杂 UFunction 形态的 thunk 解包契约**:generator 必须按签名 emit 解包代码,覆盖:(a) **out-param**(`int32&` / `UPARAM(ref)` 等):BPVM 走 `FFrame::OutParms` 链表,raw thunk 路径不走 FFrame,因此 out-param 被作为 `void**` Args 槽传出,AS Runtime 桥接 hook 在 thunk 返回后把 `Args[i]` 指向的内存写回 asIScriptGeneric 对应 out-param 槽;(b) **static 函数**:`Self == nullptr`,thunk 体走 `T::Func(...)`,AS Runtime 端走 `BindGlobalFunction` 而非 `BindMethodDirect`;(c) **WorldContextObject 隐式参数**:由 generator 在 emit 时显式作为 args 槽,AS Runtime 桥接 hook 从 AS 调用上下文拿对象填入;(d) **`const` 修饰**:thunk 体走 `static_cast<const T*>(Self)->Func()`,AS 端 method declaration 带 `const`。
- **聚合初始化非法 → ctor 形态**:`IModularFeature` 有虚析构,派生类**不是 aggregate**,**不能用 `static FFeature GFeature = { ... };` brace-init**。所有 emit cpp 与 AS Runtime 公共头中 `FAngelscriptCrossModuleFeature` 必须带 ctor,实例化用 `static FFeature GFeature(GTable, UE_ARRAY_COUNT(GTable), TEXT("Engine"), 0xA5C0DE01u);`。
- **LayoutVersion bump 契约**:任何对 `FAngelscriptCrossModuleEntry` POD 段、`FAngelscriptCrossModuleFeature` POD 段的 add/remove/reorder/widen/narrow 字段 **MUST** bump `LayoutVersionExpected`。值由 `Plugins/Angelscript/Source/AngelscriptUHTTool/cross-module-layout-version.txt`(单一 token 文件)托管,generator C# 与 AS Runtime 公共头都从该文件读,**两端不可能再漂移**。
- **stale shard 清理跨所有 supported module**:现有 `DeleteStaleOutputs` 只 enumerate AngelscriptRuntime 自己的 output dir;新方案下 cross-module shard 散落在 N 个引擎模块下,某函数被加 `NotInAngelscript` 后旧 shard 不删 → UBT 仍编 → 重复定义 link error。`AngelscriptFunctionTableCodeGenerator.DeleteStaleOutputs` 必须扩展到所有 supported module 的 OutputDirectory。
- **同模块多 shard = 多个 feature 实例**:大模块按 `MaxEntriesPerShard = 256` 分片,每个 shard 文件有自己的 anonymous `GFeature` + `GAutoReg`,**同一引擎模块在 IModularFeatures 注册表中会出现多条同名 feature**。AS Runtime 按 feature 实例迭代而非按 ModuleName 去重;ModuleName 字段仅用于诊断/日志。
- **Shutdown 时序安全**:`~FAutoReg()` 在 DLL 卸载时调 `UnregisterModularFeature`,但 `IModularFeatures` 是 Meyers singleton,跨 DLL 析构顺序未定义;若 Core 单例已销毁,`IModularFeatures::Get()` 解引用就崩。AS Runtime 与 emit cpp 双方在调 `Unregister`/`Get` 前必须 `IModularFeatures::IsAvailable()`(若 UE 暴露)兜底,或在 `FCoreDelegates::OnPreExit` 主动 unsubscribe + 在 `~FAutoReg` 包裹安全调用。
- **`OnModularFeatureRegistered` 线程安全**:UE 不保证该委托在 main thread 触发(动态加载的 plugin 可能在 worker thread 调 `RegisterModularFeature`)。AS Runtime 的回调里写 `ClassFuncMaps` 与 AS Engine `BindMethodDirect` 都 *不* 是 thread-safe(参考 `Bind_BlueprintCallable.cpp:324` 注释"AS Engine register half (must run on GameThread)")。回调必须 `AsyncTask(ENamedThreads::GameThread, ...)` 把工作 marshal 到主线程。
- **Modular / Monolithic 同路径**:Editor(Modular)与 Shipping(Monolithic)两种 build 都走 `IModularFeatures` 自注册路径 — 该机制不依赖 PE 导出表,Monolithic 下也工作。Launcher / 不重编引擎用户的目标模块根本没编出 cross-module shard,运行时 `IModularFeatures` 拉空,沿用现有 `BlueprintCallableReflectiveFallback` 兜底;**双模式选择不靠编译宏,自然落地**。
- **三道 ABI 防护**:(a) 编译期 `static_assert(sizeof(FAngelscriptCrossModuleEntry) == ...)` 双端等价;(b) `LayoutVersion` magic 字段不一致即 runtime warn + skip;(c) feature 进 AS Runtime 时核 `Count >= 0`/`Table != nullptr`/`ModuleName != nullptr`,失败 skip,不 crash。
- **Day-0 link probe 作为不可逾越的前置**:Phase 0 第一项任务在 Engine 模块手写一个最小 IModularFeatures self-register cpp,验证 (i) UBT 是否纳编目标模块 OutputDirectory 下额外 .cpp、(ii) AS Runtime 端能否通过 `GetModularFeatureImplementations` 在 Late+60 拿到。失败即 STOP,不继续后续工程化。

不属于本 change:`UASFunction` 内部 dispatch / StaticJIT / 反射 fallback 内部实现 — 这些已由 `uasfunction-runtime-dispatch-coverage`、`uasfunction-dispatch-matrix-and-jit-paths` 覆盖。

## Capabilities

### New Capabilities
- `as-blueprintcallable-direct-bind`: 定义 `BlueprintCallable` UFunction 在 AS 中按"UHT 跨模块 emit + IModularFeatures 中央会合点 + POD payload(ctor 实例化) + raw thunk + AS Runtime 通用 generic hook 桥接"路径直绑的可观测行为、Modular/Monolithic 同路径选择规则、三道 ABI 防护要求、RPC/Net 函数的 skip 契约、out-param/static/WorldContext/const 等复杂形态的解包语义、stale shard 跨模块清理责任、Shutdown 时序安全、`OnModularFeatureRegistered` 线程 marshal 要求,以及 `unexported-symbol` 类目的覆盖率验收。

### Modified Capabilities
<!-- 不修改既有 spec 的 requirements。`uasfunction-*` 谱写的是 UASFunction 内部 dispatch,而本 change 是 AS 调用外部 UFunction 的绑定路径,语义边界不交叉。 -->

## Impact

- **代码**:
  - `Plugins/Angelscript/Source/AngelscriptUHTTool/AngelscriptFunctionTableExporter.cs`(扩展输出策略,按 module 分发)
  - `Plugins/Angelscript/Source/AngelscriptUHTTool/AngelscriptFunctionTableCodeGenerator.cs`(新增 cross-module shard 形态:IModularFeatures 自注册 + raw thunk + POD 表;CSV/Summary 区分 Direct / CrossModule / Stub)
  - `Plugins/Angelscript/Source/AngelscriptUHTTool/AngelscriptHeaderSignatureResolver.cs`(去掉 `unexported-symbol` 判定中的"跨模块即视为不可达")
  - `Plugins/Angelscript/Source/AngelscriptRuntime/Public/UHT/AngelscriptCrossModuleBindings.h`(新:POD layout、reader、`LayoutVersionExpected`、双端 `static_assert`)
  - `Plugins/Angelscript/Source/AngelscriptRuntime/Binds/Bind_CrossModuleDirect.cpp`(新,`EOrder::Late + 60`,通用 generic hook + `OnModularFeatureRegistered` 订阅)
  - `Plugins/Angelscript/Source/AngelscriptRuntime/Binds/Bind_BlueprintCallable.cpp`(走原有逻辑,仅依赖 `ClassFuncMaps` 已被填充的事实)
- **`AngelscriptRuntime.Build.cs` 不新增任何引擎模块依赖**(本 change 的硬性约束;`Features/IModularFeatures.h` 来自 Core,Core 已在依赖列表)。
- **生成物**:`AS_FunctionTable_<Module>_CrossModule_NNN.cpp` 落到目标模块的 `Intermediate/Build/.../<Module>/` 下;同一模块多 shard = 多个 feature 实例;`AS_FunctionTable_Summary.json` 拓展 `crossModuleEntries` 字段;`AS_FunctionTable_Entries.csv` `EntryKind` 增加 `CrossModule` 取值;`AS_FunctionTable_ModuleSummary.csv` 增加 `CrossModuleEntries` 列;`AS_FunctionTable_SkippedReasonSummary.csv` 新增 `rpc-net-function`、`target-module-disabled` 等 reason 字符串。
- **新增源文件**:`Plugins/Angelscript/Source/AngelscriptUHTTool/cross-module-layout-version.txt`(单 token,layout 契约的"single source of truth";generator C# 与 AS Runtime 公共头都从这里读)。
- **stale shard 清理边界扩张**:`AngelscriptFunctionTableCodeGenerator.DeleteStaleOutputs` 改为 enumerate 所有 supported module 的 OutputDirectory(不仅 AngelscriptRuntime),否则旧 cross-module shard 残留会 link error。
- **构建**:Source-build target 链接器必须能纳编目标模块下的额外 `.cpp`(Day-0 probe 验证)。Launcher target 与 Monolithic Shipping target 均不要求引擎模块产生 cross-module shard:目标模块没编出该 .cpp 时 `IModularFeatures` 自然拉不到 feature,反射 fallback 兜底。
- **测试**:新增 `Angelscript.TestModule.Bindings.CrossModuleDirectBind.*` 一组 CQTest 用例(`Plugins/Angelscript/Source/AngelscriptTest/Bindings/`)、`Angelscript.CppTests.UHTToolResolver.*` headless 单测(`Plugins/Angelscript/Source/AngelscriptTest/UHTTool/`),覆盖直绑生效、行为等价、ABI 防护三道、`OnModularFeatureRegistered` 后到模块、Modular/Monolithic 双 build。
- **文档**:更新 `Documents/Guides/BindGapAuditMatrix.md` 中 stubRate 基线;在 `Documents/Guides/TestPerformance.md` 增加 cross-module 直绑 micro-bench 一节(目标:相对反射 cached 至少减少 30% per-call 耗时)。
- **风险**:(a) UBT 是否自动纳编目标模块下额外 .cpp — 由 Day-0 probe 关门;(b) 双端 layout 漂移 — 由三道防护(`static_assert` / `LayoutVersion` magic 由 `cross-module-layout-version.txt` 单源管理 / runtime 校验)闭合;(c) 静态构造期 IModularFeatures 是否就绪 — Core 早于一切引擎模块加载,Day-0 probe 同时验证;(d) Late+60 之后才加载的模块 — 由 `OnModularFeatureRegistered` 委托接管,回调 marshal 到 GameThread 处理;(e) RPC/Net 函数误走直绑 — 由 generator `FUNC_Net` skip + 回归测试守住;(f) DLL 卸载时序 — `IsAvailable` + `OnPreExit` unsubscribe 双重保险;(g) stale shard 跨模块残留 — `DeleteStaleOutputs` 扩展到所有 supported module。
