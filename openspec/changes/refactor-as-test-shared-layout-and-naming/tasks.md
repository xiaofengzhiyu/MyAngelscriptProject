## 1. Phase 1 — Header 拆分 + Cleanup 收敛 + 别名退役 + 散落 helper TODO 标记

- [x] 1.1 <!-- Non-TDD --> 创建 `Plugins/Angelscript/Source/AngelscriptTest/Shared/AngelscriptTestEngineAcquisition.h`（纯 .h，原 inline 实现保留；`.cpp` 拆分推迟到 follow-up），迁入 `AngelscriptTestUtilities.h` 行段 32-247（Engine 获取/创建：`CreateBareScriptEngine`、`CreateScriptScanFreeFullEngineForTesting`、`CreateScriptScanFreeEngineForTesting`、`CreateIsolatedFullEngine`、`CreateIsolatedCloneEngine`、`GetOrCreateSharedCloneEngine`、`AcquireTransientFullTestEngine`、`AcquireFreshSharedCloneEngine` 等）加上 468-689 段共享引擎重置/调试日志（`ResetSharedCloneEngine` / `LogSharedEngineDebugState` / `AcquireCleanSharedCloneEngine`）。在头顶加 10-20 行块注释。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-1-1 -TimeoutMs 1800000 -NoXGE`. ✅ 落地为 `AngelscriptTestEngineAcquisition.h` 491 行（pre-task-1.8）；UBT incr build 17.3s 通过。
- [x] 1.2 <!-- Non-TDD --> 创建 `Shared/AngelscriptTestEngineCleanup.h`，迁入 `AngelscriptTestUtilities.h` 行段 250-407（`CleanupDetachedASTypesForGarbageCollection` + `FDetachedASTypeCleanupResult` + `WITH_EDITOR` 区块的 `BlueprintActionDatabase` / `K2Node_GetSubsystem` 依赖）。**所有** `BlueprintActionDatabase.h` / `K2Node_*.h` 的 `#include` 仅出现在本头。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-1-2 -TimeoutMs 1800000 -NoXGE`. ✅ 落地为 193 行；`rg` 验证 3 个编辑器头在 Shared/ 内仅 Cleanup.h 1 处命中。
- [x] 1.3 <!-- Non-TDD --> 创建 `Shared/AngelscriptTestMemoryProbe.h`，迁入 `AngelscriptTestUtilities.h` 行段 409-466（`SampleBindFreeMem` / `AcquireTransientFullTestEngineWithProbe`）。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-1-3 -TimeoutMs 1800000 -NoXGE`. ✅ 落地为 109 行（include Acquisition + Cleanup）。
- [x] 1.4 <!-- Non-TDD --> 创建 `Shared/AngelscriptTestModuleBuilder.h`，迁入 `AngelscriptTestUtilities.h` 行段 693-870（`BuildModule` / `GetFunctionByDecl` / `FScopedAutomaticImportsOverride` / `ReportCompileDiagnostics`）。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-1-4 -TimeoutMs 1800000 -NoXGE`. ✅ 落地为 219 行。
- [x] 1.5 <!-- Non-TDD --> 创建 `Shared/AngelscriptTestExecute.h`（**直接以最终名落地**，不走 `AngelscriptTestExecution.h` 中转），迁入 `AngelscriptTestUtilities.h` 行段 873-1015（`ExecuteIntFunction` / `ExecuteIntFunctionExpectingScriptException` / `ExecuteInt64Function`）。本 Phase 文件**仅含** Utilities.h 873-1015 段，符号名暂保持原状。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-1-5 -TimeoutMs 1800000 -NoXGE`. ✅ 落地为 176 行，等待 Phase 2/3 注入 `FAngelscriptTestExecutor` + `Execute*` 族。
- [x] 1.6 <!-- Non-TDD --> 创建 `Shared/AngelscriptTestFixture.h`，迁入 `AngelscriptTestUtilities.h` 行段 1016+（`FAngelscriptTestFixture`）。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-1-6 -TimeoutMs 1800000 -NoXGE`. ✅ 落地为 121 行，是 Shared/* 内**唯一**依赖 5 个兄弟头的复合 facade。
- [x] 1.7 <!-- Non-TDD --> 把 `Shared/AngelscriptTestUtilities.h` 缩成 ~40 行 umbrella header：删除全部内联实现，仅保留 `#include` 上面 6 个新头 + 现有 `AngelscriptTestEngine.h` + `Misc/AutomationTest.h`，加 umbrella 用途说明注释。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-1-7 -TimeoutMs 1800000 -NoXGE`. ✅ 缩瘦为 61 行（多出来的行为 ~50 行 docblock 解释 umbrella 角色 + 6 头 + 305 个 transitive consumer 不动）。UBT incr build 17.3s 通过、Result: Succeeded、0 errors。**Commit A 落地**：submodule `621cb1b` + root `055e5fd`。
- [x] 1.8 <!-- Non-TDD --> 退役 4 个纯转发别名 `GetSharedTestEngine` / `GetResetSharedTestEngine` / `AcquireFreshSharedCloneEngine` / `ResetSharedInitializedTestEngine`：在 `AngelscriptTestEngineAcquisition.h` 内删除这 4 个 wrapper；批量替换 `AngelscriptTest/` 模块内全部 callsite（**实际 19 处，跨 9 个测试 `.cpp`**，比原估的 ~46 处低 2/3 — 历史 `git grep` 把同名注释/字符串也算进去了）为 `GetOrCreateSharedCloneEngine` / `AcquireCleanSharedCloneEngine` / `ResetSharedCloneEngine` / `DestroySharedAndStrayGlobalTestEngine() + AcquireCleanSharedCloneEngine()` 显式 pair。Verify with `rg -n "GetSharedTestEngine\|GetResetSharedTestEngine\|AcquireFreshSharedCloneEngine\|ResetSharedInitializedTestEngine" Plugins\Angelscript\Source` 应返回 0 行，然后 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-1-8 -TimeoutMs 1800000 -NoXGE`. ✅ 同时 helper-test 文件配套处理：**删除** alias-existence regression `FAngelscriptTestEngineHelperGetSharedTestEngineAliasesSharedCloneTest`（行为已被 `SharedEngineNeverAttachesToProduction` 覆盖），**重命名** `FAngelscriptTestEngineHelperGetResetSharedTestEngineResetsSharedStateTest` → `FAngelscriptTestEngineHelperAcquireCleanSharedCloneEngineResetsModulesTest`（automation 路径从 `...GetResetSharedTestEngineResetsSharedState` → `...AcquireCleanSharedCloneEngineResetsModules`，reset-then-discard-modules 不变性继续在测试中验证）。`rg` 真实调用 0 命中（仅 Acquisition.h 历史注释 + README 文档表述）。
- [x] 1.9 <!-- Non-TDD --> 在以下 Bindings/*.cpp 文件头加 `// TODO(refactor-as-test-shared-layout-and-naming): migrate <helper-list> to Shared/AngelscriptTestExecute.h` 标记：`AngelscriptMathBindingsTests.cpp`、`AngelscriptMathOrientationFunctionLibraryTests.cpp`、`AngelscriptMathFunctionLibraryTests.cpp`、`AngelscriptCurveFloatBindingsTests.cpp`、`AngelscriptWorldFunctionBindingsTests.cpp` 等含有私有 `ExecuteValueFunction` / `ExecuteIntFunctionWithAddressArg` / `ExecuteIntFunction` / `ExecuteFunctionExpectingException` / `ExecuteBoolFunction` 的文件。先 `rg` 扫描确定完整文件清单。Verify with `rg -n "static (bool|template) .*Execute\w+Function" Plugins\Angelscript\Source\AngelscriptTest\Bindings`. ✅ 实际清单 **11 个文件 / 14 个 helpers**（比原列出的 5 个更全）：`AngelscriptAssetRegistryBindingsTests.cpp`、`AngelscriptCollisionParamsBindingsTests.cpp`、`AngelscriptCurveFunctionLibraryTests.cpp`、`AngelscriptJsonBindingsTests.cpp`、`AngelscriptMathBindingsTests.cpp`、`AngelscriptMathOrientationBindingsTests.cpp`、`AngelscriptScriptFunctionLibraryTests.cpp`、`AngelscriptWorldCollisionBindingsTests.cpp`、`AngelscriptWorldCollisionFunctionLibraryComponentTests.cpp`、`AngelscriptWorldCollisionFunctionLibraryTraceTests.cpp`、`AngelscriptWorldFunctionLibraryTests.cpp`。每个文件在 helper 定义前 prepend 一行 TODO 注释覆盖该文件全部待迁移项。
- [x] 1.10 <!-- Non-TDD --> 创建 `Shared/README.md`：1 屏内列出 umbrella + 6 个主题头 + bindings 簇 sibling 头 + legacy forward 头 + 散落 helper TODO 提示，给出新代码推荐入口（`AngelscriptTestExecute.h`）+ 旧名 alias 兼容说明。 ✅ ~120 行（推荐入口表 / 完整 header inventory / Bindings 簇分类 / alias retirement table / scattered helper TODO inventory / scope guards）。
- [x] 1.11 <!-- Non-TDD --> Phase 1 收口验证：`rg` 旧 4 别名 0 命中、`rg` 编辑器头依赖仅在 Cleanup.h 1 处命中、`rg` `BlueprintActionDatabase\.h` 仅 `AngelscriptTestEngineCleanup.h` 1 处命中。运行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTestSuite.ps1 -Label as-test-shared-layout-phase1 -TimeoutMs 1800000`，确认 `275/275` C++ baseline + `301/301` ASSDK 子套不退化。 ✅ rg 三项验证全过。**Fast suite 跑出 1834/1834 全 PASS、0 fail、0 skip**（实际用 `RunTestSuiteFast.ps1` 替代 `RunTestSuite.ps1` 加速到 ~8min）：`AngelScriptSDK = 301/301` ✓、`Bindings = 260/260`、`Shared = 37/37`（含重命名的 helper test）、`HotReload = 40/40`、`Functional = 104/104`、`ClassGenerator = 41/41`、`Core = 39/39` — 受 task 1.8 callsite 改动直接波及的全部 bucket 全部通过，**零回归**。**Commit B 落地**：submodule `06702b1` + root `c91d516`。

## 2. Phase 2 — 合并 InvokerHeader + AssertionsHeader 进 Execute.h（保留旧符号名）

**Implementation note**：tasks 2.1-2.4 物理上必须**单批次原子完成**——若先 append 内容到 Execute.h 但暂不把旧两头改为 forward stub，会立即产生 ODR 重复定义而无法 build verify。tasks.md 原文按 4 段拆分 build verify 的设计在写 spec 时是可行的（每段独立闭环），但实际操作时必须把 4 段 ed 操作合成一次提交、build verify 推到 2.4 完成后做一次（语义等价）。

- [x] 2.1 <!-- Non-TDD --> 把 `Shared/AngelscriptGlobalFunctionInvoker.h`（443 行）全部内容（`ResolveFunctionByDecl` / `ResolveFunctionByName` / `FASGlobalFunctionInvoker` 类）追加进 `Shared/AngelscriptTestExecute.h`，**保留原命名空间 `AngelscriptReflectiveAccess::` 与原类名 `FASGlobalFunctionInvoker` 不变**。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-2-1 -TimeoutMs 1800000 -NoXGE`. ✅ verbatim 内容（namespace + 2 个 Resolve* + 完整 FASGlobalFunctionInvoker 类含所有 AddArg/Call/CallAndReturn/ReadReturnStruct 成员）落地。build verify 合并到 2.4 同批次。
- [x] 2.2 <!-- Non-TDD --> 把 `Shared/AngelscriptBindingsAssertions.h`（409 行）全部内容（9 个 `ExpectGlobal*` + `ExpectBindingCompileFailure` + `Detail::TraceCase`）追加进 `Shared/AngelscriptTestExecute.h`，**保留原命名空间 `AngelscriptTestBindings::` 与原函数名不变**。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-2-2 -TimeoutMs 1800000 -NoXGE`. ✅ verbatim 内容（`#if WITH_DEV_AUTOMATION_TESTS` guard + `Detail::TraceCase` + `ExpectBindingCompileFailure` + 9 个 `ExpectGlobal*` 自由函数 + 1 个 batch struct `FExpectedGlobalInt`）落地。build verify 合并到 2.4 同批次。
- [x] 2.3 <!-- Non-TDD --> 把 `Shared/AngelscriptGlobalFunctionInvoker.h` 改成 ~3 行 forward 头：仅含 `#pragma once` + `#include "AngelscriptTestExecute.h"` + 一行说明注释。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-2-3 -TimeoutMs 1800000 -NoXGE`. ✅ 缩瘦为 9 行（多出来的是 ~7 行 docblock 解释 stub 角色 + 1 行 `#include`）。build verify 合并到 2.4 同批次。
- [x] 2.4 <!-- Non-TDD --> 把 `Shared/AngelscriptBindingsAssertions.h` 改成 ~3 行 forward 头：仅含 `#pragma once` + `#include "AngelscriptTestExecute.h"` + 一行说明注释。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-2-4 -TimeoutMs 1800000 -NoXGE`. ✅ 缩瘦为 13 行（含 stub 用途 docblock 列出全部 10 个迁移符号名 + 强调 `WITH_DEV_AUTOMATION_TESTS` guard 已随内容搬走 + 1 行 `#include`）。**首次 build verify 暴露循环依赖**：`Helper.h → umbrella → Execute.h → Helper.h #pragma once skip` 导致 Execute.h 内 `ExpectBindingCompileFailure` 解析时 `AngelscriptTestSupport::FAngelscriptCompileTraceSummary` 不可见。**修复**：删除 Helper.h line 4 的 `#include "Shared/AngelscriptTestUtilities.h"`（Phase 1 拆分后已是 vestigial — Helper.h 自身 declarations 不需要 umbrella 任何符号），改为 `AngelscriptEngine.h` + `CoreMinimal.h` 直接 include + 一段长注释说明删除原因 + 305 个 consumer 兼容性提示。重新 build：UBT incr 17.3s 通过、Result: Succeeded、0 errors。
- [x] 2.5 <!-- Non-TDD --> Phase 2 收口验证：`rg -n "ExpectGlobalInt|ExpectGlobalBool|ExpectGlobalDouble|FASGlobalFunctionInvoker" Plugins\Angelscript\Source\AngelscriptTest\Bindings` 命中数应与本 Phase 启动前一致（命名未改、callsite 未动）；`rg -n "namespace AngelscriptReflectiveAccess|namespace AngelscriptTestBindings" Plugins\Angelscript\Source\AngelscriptTest\Shared` 应在 `AngelscriptTestExecute.h` 内命中、在原两头不再命中。运行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTestSuite.ps1 -Label as-test-shared-layout-phase2 -TimeoutMs 1800000`，确认全套 baseline 不退化。 ✅ 4 个 rg gate 全过：gate 1 (旧两头 0 namespace 块) + gate 2 (Execute.h 含 3 个 namespace 起始于 line 65/231/666) + gate 3 (旧两头分别 9/13 行 forward stub 含 Execute.h include) + gate 4 (Helper.h 0 umbrella include)。**Fast suite 跑出 1834/1834 全 PASS、0 fail、0 skip**，**Δ vs Phase 1 baseline = 0**（test 数量完全一致）：`Bindings = 260/260`、`Shared = 37/37`、`AngelScriptSDK = 301/301` 等所有 36 个 bucket 全部通过。**Phase 2 commit 落地**：submodule + root gitlink。

## 3. Phase 3 — Execute* 新命名族 + executor 类改名 + 旧符号 inline alias

- [x] 3.1 <!-- Non-TDD --> 在 `Shared/AngelscriptTestExecute.h` 内定义新类 `FAngelscriptTestExecutor`：可以把原 `FASGlobalFunctionInvoker` 重命名为 `FAngelscriptTestExecutor`，在原命名空间外（推荐顶级命名空间或 `AngelscriptTest::`）再定义；同步把 `.Call()` → `.Execute()`、`.CallAndReturn<T>()` → `.ExecuteAndGet<T>()`、`.ReadReturnStruct<T>()` → `.ExecuteAndExtractStruct<T>()`；保留旧成员名（`.Call` / `.CallAndReturn<T>` / `.ReadReturnStruct<T>`）作为 inline 转发方法。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-3-1 -TimeoutMs 1800000 -NoXGE`. **DONE**: Per design decision `ns-angelscripttest`, the new class lives in `namespace AngelscriptTest`. Old members `.Call` / `.CallAndReturn<R>` / `.ReadReturnStruct<T>` are retained as inline forwarders to `.Execute` / `.ExecuteAndGet<R>` / `.ExecuteAndExtractStruct<T>`. Build verify combined with 3.2-3.4 into a single atomic file rewrite (header refactor cannot be physically split into 4 builds without ODR breakage).
- [x] 3.2 <!-- Non-TDD --> 在 `Shared/AngelscriptTestExecute.h` 内加 `using FASGlobalFunctionInvoker = FAngelscriptTestExecutor;`（在原命名空间 `AngelscriptReflectiveAccess::` 内），确保旧 callsite `AngelscriptReflectiveAccess::FASGlobalFunctionInvoker` 仍可用。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-3-2 -TimeoutMs 1800000 -NoXGE`. **DONE**: Added `using FASGlobalFunctionInvoker = AngelscriptTest::FAngelscriptTestExecutor;` inside `namespace AngelscriptReflectiveAccess`. 13 files in `Bindings/` continue to compile through this alias without source changes. Old free functions `ResolveFunctionByDecl` / `ResolveFunctionByName` retained as inline forwarders in the same namespace. Build verify rolled up into 3.4 atomic build.
- [x] 3.3 <!-- Non-TDD --> 在 `Shared/AngelscriptTestExecute.h` 内增加新 `Execute*` 自由函数族（实现而非别名）：
  - `ExecuteAndExpectInt` / `ExecuteAndExpectBool` / `ExecuteAndExpectDouble`（相等断言）
  - `ExecuteAndExpectNearFloat` / `ExecuteAndExpectNearDouble`（容差断言）
  - `ExecuteAndExpectIntAtLeast`（下界断言）
  - `ExecuteBatchAndExpectInt`（批量同 fn）
  - `ExecuteAndValidate<T>`（自定义校验）
  - `ExecuteAndExpectException`（脚本异常）
  - `CompileAndExpectFailure`（compile-side 独立族）
  
  每个新函数内部直接走 `FAngelscriptTestExecutor` API，不再调用旧 `ExpectGlobal*` 名。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-3-3 -TimeoutMs 1800000 -NoXGE`. **DONE**: All 10 new free functions defined inside `namespace AngelscriptTest`, each implemented directly on top of `FAngelscriptTestExecutor::Execute` / `ExecuteAndGet<R>` / `ExecuteAndExtractStruct<T>`. Per design decision `batch-alias`, the batch struct is `FExpectedInt` with `using FExpectedGlobalInt = AngelscriptTest::FExpectedInt;` kept in `AngelscriptTestBindings` for backward compat. Per design decision `double-strict`, `ExecuteAndExpectDouble` uses bitwise equality (NaN-safe via memcmp) while `ExecuteAndExpectNearDouble` is the tolerance path. Build verify rolled up into 3.4 atomic build.
- [x] 3.4 <!-- Non-TDD --> 把旧 9 个 `ExpectGlobal*` 函数 + `ExpectBindingCompileFailure` + Utilities.h 873-1015 段的 `ExecuteIntFunction` / `ExecuteIntFunctionExpectingScriptException` / `ExecuteInt64Function` 改写为 inline 转发到新 `Execute*` / `CompileAndExpectFailure` 的薄壳；签名保持完全一致。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-3-4 -TimeoutMs 1800000 -NoXGE`. **DONE**: All 9 `ExpectGlobal*` helpers + `ExpectBindingCompileFailure` + `ExecuteFunctionExpectingScriptException` in `AngelscriptTestBindings` rewritten as inline forwarders to their `AngelscriptTest::` counterparts. Per design decision `leg-keep`, the `AngelscriptTestSupport::ExecuteIntFunction` / `ExecuteIntFunctionExpectingScriptException` / `ExecuteInt64Function` trio is **preserved verbatim** (not rewritten as forwarders) — kept as the documented legacy hop for tooling that has not yet migrated. Build verify (atomic, covers 3.1-3.4 together): `RunBuild.ps1 -Label as-test-shared-layout-3-build -NoXGE` → **Result: Succeeded** (only Execute.h recompiles transitively, dependents unchanged). Fix-up note: had to add `using AngelscriptTestBindings::FBindingsCoverageProfile / FormatCaseLabel / MakeCoverageModuleName;` declarations inside `namespace AngelscriptTest` to let `Execute*` family reference coverage types without fully qualifying every site (Bindings/* call sites work both forms).
- [x] 3.5 <!-- Non-TDD --> Phase 3 收口验证：`rg -n "FAngelscriptTestExecutor" Plugins\Angelscript\Source\AngelscriptTest\Shared\AngelscriptTestExecute.h` 应命中类定义；`rg -n "ExecuteAndExpect|ExecuteAndGet|ExecuteAndValidate|ExecuteBatchAndExpect|CompileAndExpectFailure" Plugins\Angelscript\Source\AngelscriptTest\Shared\AngelscriptTestExecute.h` 应命中所有新名；旧 callsite 编译通过。运行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTestSuite.ps1 -Label as-test-shared-layout-phase3 -TimeoutMs 1800000`，确认全套 baseline 不退化。 **DONE**:
  - Gate 1 (new names present in Execute.h): all 13 new symbols hit — `FAngelscriptTestExecutor`(38), `ExecuteAndExpectInt`(11), `ExecuteAndExpectBool`(9), `ExecuteAndExpectDouble`(2), `ExecuteAndExpectNearFloat`(5), `ExecuteAndExpectNearDouble`(6), `ExecuteAndExpectIntAtLeast`(5), `ExecuteBatchAndExpectInt`(7), `ExecuteAndValidate`(5), `ExecuteAndExpectException`(6), `CompileAndExpectFailure`(5), `ExecuteAndGet`(14), `ExecuteAndExtractStruct`(6).
  - Gate 2 (old call sites unchanged in Bindings/): `FASGlobalFunctionInvoker` 13 files, `ExpectGlobalInt` 63 files, `ExpectGlobalInts` 14 files, `ExpectGlobalReturnBool` 5 files, `ExpectGlobalReturnFloat` 3 files, `ExpectGlobalReturnCustom` 1 file, `ExpectGlobalIntAtLeast` 3 files, `ExecuteFunctionExpectingScriptException` 10 files, `ExpectBindingCompileFailure` 2 files — all compile through forwarders without source changes. New-name call sites in Bindings/ = 0 (call-site migration is deferred to Phase 4 + followups).
  - Gate 3 (RunTestSuiteFast): label `as-test-shared-layout-phase3`, 36 shards completed, **1834 / 1834 PASS (100%), 0 failed shards** — baseline not regressed. (Aggregation step hit known `ConvertTo-AngelscriptWorkerPlanJson` PowerShell aggregator bug; per-shard `[done]` lines all show `exit=0 tests=N/N`, manually aggregated identically to Phase 1 / Phase 2 workflow.)

## 4. Phase 4 — 更新示例 + 新建 followups.md

- [ ] 4.1 <!-- Non-TDD --> 更新 `Shared/AngelscriptBindingsExampleSection.h`：把 80 行示例内的 `ExpectGlobal*` / `FASGlobalFunctionInvoker` callsite 全部贴新命名（`ExecuteAndExpect*` / `FAngelscriptTestExecutor`）作为新命名的官方示范。若已包含 `AngelscriptBindingsExampleSectionTests.cpp` 内对应 callsite 也同步更新。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-4-1 -TimeoutMs 1800000 -NoXGE`.
- [ ] 4.2 <!-- Non-TDD --> 创建 `openspec/changes/refactor-as-test-shared-layout-and-naming/followups.md`，登记 8 项待清理工作（参见本目录 `followups.md` 草稿）：Bindings/*.cpp callsite 迁移、散落 helper 收编、Profile 实例变量名重命名、Profile 字段去重、AS namespace 改写、兼容层删除、文档同步、umbrella header 进一步瘦身。
- [ ] 4.3 <!-- Non-TDD --> Phase 4 收口验证：`Shared/AngelscriptBindingsExampleSection.h` 内不再出现旧命名（除注释外）；`followups.md` 8 项条目齐全。运行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTestSuite.ps1 -Label as-test-shared-layout-phase4 -TimeoutMs 1800000`。

## 5. Phase 5 — FBindingsCoverageProfile 双字段并存

- [ ] 5.1 <!-- Non-TDD --> 修改 `Shared/AngelscriptBindingsCoverage.h`：在 `FBindingsCoverageProfile` struct 内新增全词字段 `BodyInstance` / `MessageDialog` / `MathOrientation` / `CollisionValidation` / `MathPlatform` / `JsonConversion`（每个对应一个旧缩写字段）；构造函数默认把缩写字段值拷贝到全词字段（fallback 路径）。Verify with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunBuild.ps1 -Label as-test-shared-layout-5-1 -TimeoutMs 1800000 -NoXGE`.
- [ ] 5.2 <!-- Non-TDD --> 找到所有 `FBindingsCoverageProfile` 字面量定义（`rg -n "FBindingsCoverageProfile\s+\w+" Plugins\Angelscript\Source\AngelscriptTest`），逐个 Profile 同步给全词字段赋值（与旧缩写字段同值）。这一步是为了让读者从任一字段读取相同值。Verify with `rg -c "BodyInstance\s*=" Plugins\Angelscript\Source\AngelscriptTest\Bindings` 命中数 ≥ `rg -c "BodyInst\s*=" Plugins\Angelscript\Source\AngelscriptTest\Bindings`.
- [ ] 5.3 <!-- Non-TDD --> 修正 `ModulePrefix = TEXT("ASMathBindings")` 唯一例外为 `ModulePrefix = TEXT("ASMath")`；同步更新 `AngelscriptMathBindingsTests.cpp` 内 `AddExpectedError(TEXT("...ASMathBindings..."))` 字符串。Verify with `rg -n "ASMathBindings" Plugins\Angelscript\Source\AngelscriptTest` 应返回 0 行。
- [ ] 5.4 <!-- Non-TDD --> Phase 5 收口验证：所有 `FBindingsCoverageProfile` 字面量已同步赋值全词字段；`ModulePrefix` 全部符合 `AS<Subject>` 格式无 `Bindings` 后缀。运行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTestSuite.ps1 -Label as-test-shared-layout-phase5 -TimeoutMs 1800000`，确认全套 baseline 不退化。

## 6. 收尾验证

- [ ] 6.1 <!-- Non-TDD --> 运行 `openspec validate refactor-as-test-shared-layout-and-naming --strict --json` 从项目根目录，确认 change 通过 OpenSpec 校验。
- [ ] 6.2 <!-- Non-TDD --> 最终全套 baseline 验证：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File Tools\RunTestSuite.ps1 -Label as-test-shared-layout-final -TimeoutMs 1800000`；与 `Documents/Guides/TestCatalog.md` 的 275/275 baseline + `301/301` ASSDK 子套对比，确认无退化、无新增 Disabled。
- [ ] 6.3 <!-- Non-TDD --> 创建提交（按 `Documents/Rules/GitCommitRule.md` 风格，单 commit 或按 Phase 拆分提交）。
